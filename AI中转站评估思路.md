假设中转站API不掺水不造假的前提下，怎样分析中转站的实际价格？

核心思路是：**不要直接比较“积分多少钱”，而是统一折算成「我花多少人民币，买到官方 API 里 1 美元的模型用量」**。

也就是固定一个指标：

**¥ / 官方 $1 用量**

这个指标最适合比较零售、中转套餐、订阅套餐、官方 API。

第一步，先冻结比较边界。否则很容易像前面那样把 GPT-5.4、GPT-5.5、标准价、长上下文价混在一起。

你每次分析前先定死这几项：

| 项目         | 必须固定                                     |
| ------------ | -------------------------------------------- |
| 模型         | 例如 GPT-5.4，不要混 GPT-5.5                 |
| 官方价格     | input / cached input / output 分别多少钱     |
| 价格口径     | 标准价、长上下文价、Batch 价分开             |
| 使用场景     | 纯聊天、长文处理、Agent coding 分开          |
| token 构成   | input、output、cache、reasoning 分开         |
| 中转计费单位 | 美元余额、积分、credit，不要默认等于官方美元 |

第二步，分两类产品算。

一类是**零售余额型**。

比如：充值人民币，到账美元余额或积分，然后按 token 消耗扣费。

这类的关键公式是：

```text
用户实际成本 = 平台扣费 × 平台单位的人民币成本

官方等值成本 = 同样 token 按官方 API 价格计算出来的美元成本

折合成本 = 用户实际成本 ÷ 官方等值成本
```

举个抽象例子：

| 项目                     | 数值            |
| ------------------------ | --------------- |
| 充值                     | ¥70             |
| 到账                     | $10 平台余额    |
| 平台余额成本             | ¥7 / 平台 $1    |
| 某次请求平台扣费         | $0.20           |
| 用户实际成本             | ¥1.40           |
| 同样 token 官方 API 成本 | $1.00           |
| 结果                     | ¥1.40 / 官方 $1 |

这个结果说明：你花 ¥1.40，买到了官方 API 里 $1 的用量。那它其实不便宜，因为官方 $1 按汇率也才约 ¥7 左右，¥1.40 是官方价的 20% 左右；但具体划不划算，还要看稳定性和安全性。

第二类是**套餐额度型**。

套餐型又要分三种，不要混。

| 套餐类型      | 怎么理解                               | 怎么算                                                       |
| ------------- | -------------------------------------- | ------------------------------------------------------------ |
| 固定美元额度  | 例如 ¥20 买 $200 额度                  | 直接 ¥20 ÷ $200 = ¥0.10 / 官方 $1，前提是这个 $200 真按官方价扣 |
| 固定积分总量  | 例如 $15 买 270000 积分                | 先把积分换成 token，再按官方价折算成官方美元                 |
| 积分池 + 恢复 | 例如 9000 上限 + 每小时恢复 + 每日重置 | 不能只看 9000，要算 30 天理论可消费积分，且区分保守/常规/极限 |

套餐型的通用公式是：

```text
套餐折合成本 = 套餐实付人民币 ÷ 套餐可覆盖的官方 API 等值美元
```

比如 UniVibe 这种，假设它说：

```text
GPT-5.4：
1500 积分 = 100万输入 token
9000 积分 = 100万输出 token
```

那你要先算：

```text
270000 积分 = 180M 输入 token
270000 积分 = 30M 输出 token
```

然后再用 **GPT-5.4 官方价格** 算官方等值。不能拿 GPT-5.5 的价格套进去。

第三步，单独算 input 和 output，不要只算一个混合价。

因为大模型官方价通常 output 比 input 贵很多。很多平台也会让 output 消耗更多积分。你至少要输出这三组结果：

| 指标           | 用途                                        |
| -------------- | ------------------------------------------- |
| 纯 input 场景  | 长文分析、代码库读取、RAG 查询              |
| 纯 output 场景 | 生成长文、生成代码、批量改写                |
| 混合场景       | 更接近日常使用，例如 80% input + 20% output |

混合场景公式：

```text
官方等值成本 =
input_M × 官方input单价
+ output_M × 官方output单价
+ cached_input_M × 官方cache单价
```

中转实际成本也按同样结构算：

```text
平台实际成本 =
input_M × 平台input单价
+ output_M × 平台output单价
+ cached_input_M × 平台cache单价
```

如果平台没有 cached input 单价，那就要默认它可能没把缓存优惠返给用户，或者没有明确说明。这个要标成风险点。

第四步，给每个中转站做四张表。

第一张：**价格表**。

| 项目         | 零售                   | 套餐 |
| ------------ | ---------------------- | ---- |
| 实付人民币   | 多少                   |      |
| 到账单位     | 美元余额 / 积分        |      |
| 单位成本     | ¥ / 平台 $ 或 ¥ / 积分 |      |
| 模型扣费规则 | input/output/cache     |      |
| 是否有恢复   | 有/无                  |      |
| 是否有上限   | 有/无                  |      |

第二张：**官方等值表**。

| 场景       | 平台可用量   | 官方等值美元 | 用户实付 | ¥ / 官方 $1 |
| ---------- | ------------ | ------------ | -------- | ----------- |
| 纯 input   | xxx M tokens | $xxx         | ¥xxx     | ¥x.xx       |
| 纯 output  | xxx M tokens | $xxx         | ¥xxx     | ¥x.xx       |
| 80/20 混合 | xxx M tokens | $xxx         | ¥xxx     | ¥x.xx       |

第三张：**风险表**。

| 风险项     | 低风险表现                           | 高风险表现                 |
| ---------- | ------------------------------------ | -------------------------- |
| 明细       | 显示模型、input、output、cache、扣费 | 只显示积分变化             |
| 失败扣费   | 失败不扣或少扣                       | 失败、空回复、重试都照扣   |
| 模型真实性 | 能看到上游模型 ID / 响应特征稳定     | 只写“GPT-5 系列”           |
| 价格变动   | 有公告、有历史记录                   | 随时改倍率                 |
| 隐私       | 支持 BYOK / 不留日志 / 有说明        | 所有请求过它服务器且无说明 |
| 稳定性     | 限流规则清楚                         | 高峰期不可用、吞请求       |
| 充值风险   | 小额可用、可退款                     | 大额充值、不到账难追       |

第四张：**结论表**。

| 结论           | 判断                                     |
| -------------- | ---------------------------------------- |
| 是否便宜       | 看 ¥ / 官方 $1                           |
| 是否稳定       | 看 10～20 次固定任务的扣费波动           |
| 是否适合作备用 | 看最低充值、可用模型、失败扣费、隐私风险 |
| 是否适合作主力 | 一般不建议，除非透明度和稳定性非常好     |

第五步，做实测，不要只看宣传页。

你可以用三组测试：

| 测试             | 目的                         |
| ---------------- | ---------------------------- |
| 极短固定 prompt  | 测有没有最低扣费、异常扣费   |
| 固定长文本摘要   | 测 input/output 换算是否稳定 |
| 固定 coding 任务 | 测 Agent 真实使用成本        |

每个测试至少跑 5 次，记录：

```text
模型
输入 token
输出 token
是否缓存
是否工具调用
平台扣费
官方等值成本
任务是否成功
是否重试
```

最后看一个比例：

```text
平台实际人民币成本 ÷ 官方等值美元成本
```

这个比例如果稳定，说明它至少可预测。
这个比例如果同类任务能飘 50% 以上，就算价格便宜，也不适合作主力。

我建议你的判断线可以这样定：

| ¥ / 官方 $1 | 评价                                   |
| ----------- | -------------------------------------- |
| > ¥7        | 比官方还贵，没意义                     |
| ¥3～¥7      | 小折扣，主要看稳定性                   |
| ¥1～¥3      | 明显便宜，可作为备用                   |
| ¥0.3～¥1    | 很便宜，但要重点看风险                 |
| ¥0.1～¥0.3  | 极便宜，默认高风险                     |
| < ¥0.1      | 不要只看价格，优先怀疑可持续性和真实性 |

最后一句最关键：

**“平台美元”不是“官方美元”，“积分”更不是“token”。所有中转站都要先折成 ¥ / 官方 $1，再谈便宜不便宜。**